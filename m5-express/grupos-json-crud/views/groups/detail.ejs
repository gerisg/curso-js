<!DOCTYPE html>
<html lang="en">
<%- include('../partials/head', { title: 'Detalle de grupo' }) %>
<body>
    <%- include('../partials/header') %>
    <main>
        <% if(group) { %>
            <%- include('group', { group }) %>
        <% } else { %>
            <h2>No encontramos ese grupo</h2>
        <% } %>
    </main>
</body>
</html>